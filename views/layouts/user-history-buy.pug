include ../components/navbar.pug
.container

    div.row
        div.col-sm-1
        div.col-sm-10
            h1 Danh sách đơn hàng
        div.col-sm-1

    div.table.row
        div.col-sm-1
        table.col-sm-10
            thead
                tr
                    th(scope='col') Mã đơn hàng
                    th(scope='col') Thông tin chi tiết
            if orders
                tbody
                    each order in orders
                        tr
                            td #{order.id}
                            td
                                -const url = '/history-buy-detail?id=' + order.id
                                a(href=url) Xem
        div.col-sm-1